const makeLogServer = require('./server');

const PATH = './log.txt';
const PORT = 30078;

const server = makeLogServer('./app');
server.listen(PORT);

module.export = {
    const logger = {
        constructor(logfile) {./})
    }
}