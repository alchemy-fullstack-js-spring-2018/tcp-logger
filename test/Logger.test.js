
const Logger = require('../lib/Logger');

describe('does message log', () => {
    
    beforeEach(done => {
        const logger = new Logger();


    it('log', () => {
        
        });
    });

});