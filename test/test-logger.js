// const assert = require('assert');
// const Logger = require('../lib/Logger');

// describe('something is logged',  () => {

//     it('Message is returned', () => {
//         const data = 
//         const message = new Logger(data);

//         assert.equal(message, `\n${new Date()} ** ${data.toString()}`);

//     })

// })